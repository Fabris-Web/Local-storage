const inviteForm = document.getElementById("inviteForm"); inviteForm.addEventListener("submit", function (e) { e.preventDefault(); const email = document.getElementById("inviteEmail").value; const role = document.getElementById("role").value; if (!role) { alert("Please select a role!"); return; } DB.addUser(email, role); alert(`${email} invited as ${role}.\nDefault password: 1234`); document.getElementById("inviteEmail").value = ""; document.getElementById("role").value = ""; });